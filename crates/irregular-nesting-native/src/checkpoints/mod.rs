//! TS counterpart: resumable checkpoint state within src/workers/algorithm/irregular/intrinsicCapacitySearch.ts and the shared-archive/capacity portfolio modules (checkpoint encoding, validation, and resume).

pub mod canonical_json;
